# Copiar a .env o configurar en el servidor. NO subir este archivo con valores reales.

# Base de datos PostgreSQL
DB_URL=jdbc:postgresql://localhost:5432/gimnasio_db?sslmode=disable
DB_USER=tu_usuario
DB_PASS=tu_contraseña

# Correo (Gmail: usar Contraseña de aplicación)
MAIL_USER=tu_correo@gmail.com
MAIL_PASSWORD=xxxx xxxx xxxx xxxx

# JWT (mínimo 32 caracteres para HMAC-SHA256)
JWT_SECRET=tu_clave_secreta_muy_larga_minimo_32_caracteres

# Opcional: ruta de imágenes para CargadorFotosDB (herramienta local, no usar en producción)
# IMAGENES_GIMNASIO_PATH=C:/Imagenes Gimnasio/

# IMPORTANTE: No subir .env con valores reales. Añadir .env al .gitignore.
